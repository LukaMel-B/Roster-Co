import 'package:flutter/material.dart';

class TaskShowDetailsCard extends StatelessWidget {
  const TaskShowDetailsCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
